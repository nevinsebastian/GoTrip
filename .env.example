# Optional. If unset at build time, the app uses https://api.gotripholiday.com (see src/config/env.ts).
# Vercel: add EXPO_PUBLIC_API_URL under Project → Settings → Environment Variables, then redeploy.
EXPO_PUBLIC_API_URL=https://api.gotripholiday.com
